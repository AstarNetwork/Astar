[workspace]
members = [
    "bin/collator",
    "runtime/shiden",
    "runtime/shibuya",
    "frame/block-reward",
    "frame/custom-signatures",
]

[profile.release]
# Astar runtime requires unwinding.
panic = "unwind"
