[workspace]
members = [
    "bin/node/cli",
    "bin/node/rpc",
    "bin/node/runtime",
    "bin/node/primitives",
]

exclude = [
    "contracts",
    "vendor",
]

[profile.release]
# Plasm runtime requires unwinding.
panic = "unwind"
