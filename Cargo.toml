[workspace]
members = [
    "bin/collator",
    "runtime/local",
    "runtime/shiden",
    "runtime/shibuya",
    "frame/block-reward",
    "frame/custom-signatures",
    "precompiles/staking",
]

[profile.release]
# Astar runtime requires unwinding.
panic = "unwind"
