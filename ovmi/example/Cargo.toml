[package]
name = "ovmi-example"
version = "0.1.0"
authors = ["satellitex <s.a.t.e.3.ths@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ovmi = { path = "../core", default_features = false }
serde_json = "1.0"

[features]
default = ["std"]
std = [
    'ovmi/std',
]
